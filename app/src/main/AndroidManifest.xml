<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.plannerkt">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />



    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".section_chat.chats.AddChatActivity" />
        <activity android:name=".section_chat.chats.MessageActivity" />
        <activity android:name=".section_notes.monthNotes.addMonthNote.AddMonthNoteActivity" />
        <activity android:name=".section_notes.fastNotes.addNote.AddNoteActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".section_notes.daysNotes.addDayNote.AddDayNoteActivity" />
        <activity android:name=".autorisation.AutorisationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>